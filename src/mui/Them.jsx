import { createTheme } from "@mui/material";

export const theme1 = createTheme({
    direction:'rtl',

    palette:{
        mode:"dark"
    },

    typography:{
        fontFamily:"vazir"
    }
})

export const theme2 = createTheme({
    direction:'rtl',

    palette:{
        mode:"light"
    },

    typography:{
        fontFamily:"vazir"
    }
})

